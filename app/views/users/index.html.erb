<h1>All users</h1>
<p><%= link_to "Add", new_user_path %></p>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <%= link_to "Edit", edit_user_path(user) %>
    </li>
  <% end %>
</ul>